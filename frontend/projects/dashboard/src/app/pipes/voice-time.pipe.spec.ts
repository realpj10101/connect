import { VoiceTimePipe } from './voice-time.pipe';

describe('VoiceTimePipe', () => {
  it('create an instance', () => {
    const pipe = new VoiceTimePipe();
    expect(pipe).toBeTruthy();
  });
});
